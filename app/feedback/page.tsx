import { InfoPage } from "@/components/talkt/info-page";

export const metadata = {
  title: "Feedback — TalkT",
};

export default function FeedbackPage() {
  return <InfoPage eyebrow="Feedback" title="Feedback" />;
}
