import { InfoPage } from "@/components/talkt/info-page";

export const metadata = {
  title: "Pricing — TalkT",
};

export default function PricingPage() {
  return <InfoPage eyebrow="Pricing" title="Pricing" />;
}
